matrix:
  include:
    - os: linux
      dist: trusty
sudo: required

language: node_js

cache:
  directories:
    - node_modules

install:
  - npm install

script:
  - if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then npm run release -- --linux; fi